var black = (function () {
  function map(arr, callback) {

  }

  function filter(arr, callback) {

  }

  function exclude(arr, callback) {

  }

  return {
    map: map,
    filter: filter,
    exclude: exclude,
  }
})();